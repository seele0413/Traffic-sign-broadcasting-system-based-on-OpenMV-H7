# 基于 OpenMV H7 的交通标志识别与语音播报系统

## 项目简介

本项目基于 OpenMV H7 实现交通标志识别，并通过 UART 串口将识别结果发送给 51 单片机。51 单片机根据不同标签控制 SYN6288 语音合成模块，实现对应交通标志的中文语音播报。

该系统采用边缘端本地识别方式，无需依赖云端算力，适合作为低成本交通辅助提醒、视障出行辅助和嵌入式视觉识别教学原型。

## 系统架构

交通标志图像 → OpenMV H7 视觉识别 → UART → 51 单片机逻辑判断 → UART → SYN6288 语音合成模块 → 扬声器播报

## 硬件清单

| 硬件模块 | 作用 |
|---|---|
| OpenMV H7 | 图像采集与交通标志识别 |
| 51 单片机 | 接收识别标签并控制语音播报 |
| SYN6288 语音模块 | 中文语音合成 |
| 扬声器 | 语音输出 |
| 杜邦线、电源模块 | 模块连接与供电 |

## 功能特性

- 基于 OpenMV H7 进行交通标志本地识别
- 支持 Edge Impulse 训练得到的模型部署
- 通过 UART 将识别标签发送给 51 单片机
- 51 单片机根据不同标签触发不同语音播报
- 支持简单的防重复播报逻辑，避免连续识别时反复播报

## 目录结构

| 路径 | 说明 |
|---|---|
| openmv/main.py | OpenMV 端识别与串口发送程序 |
| openmv/labels.txt | 模型标签文件 |
| openmv/trained.tflite | 训练后的模型文件 |
| 51/HELLO/syn6288.c | 51 单片机主程序与 SYN6288 控制逻辑 |
| 51/HELLO/syn6288.h | SYN6288 相关函数声明 |
| 51/HELLO/uart.c | 串口发送函数 |
| 51/HELLO/delay.c | 延时函数 |
| 51/HELLO/Hello.uvproj | Keil 工程文件 |

## 标签说明

当前 OpenMV 端通过 UART 发送单字符标签：

| 标签 | 含义 |
|---|---|
| n | 禁止鸣笛 |
| x | 禁止停车 |
| e | 限速 80 |

## 使用方法

### 1. OpenMV 端部署

将以下文件复制到 OpenMV H7：

- openmv/main.py
- openmv/labels.txt
- openmv/trained.tflite

然后在 OpenMV IDE 中运行 main.py。

### 2. 51 单片机端烧录

使用 Keil 打开 51/HELLO/Hello.uvproj，编译后将程序烧录到 51 单片机。

### 3. 模块连接

- OpenMV TX 接 51 单片机 RXD
- 51 单片机 TXD 接 SYN6288 RXD
- 各模块 GND 需要共地
- SYN6288 输出端连接扬声器

## 当前不足

- UART 通信协议较简单，目前仅发送单字符标签
- 缺少帧头、校验位和置信度字段
- 51 单片机资源有限，不适合复杂任务调度
- README 和接线文档仍需进一步完善

## 后续优化方向

- [ ] 设计更稳定的 UART 帧协议，加入帧头、标志 ID、置信度和校验位
- [ ] 增加识别置信度过滤，减少误触发
- [ ] 增加标签冷却时间和播报优先级
- [ ] 使用 STM32 或 ESP32 替换 51 单片机
- [ ] 引入 FreeRTOS，实现接收、判断、播报等任务拆分
- [ ] 基于 MQTT 上传识别日志，实现物联网扩展
- [ ] 增加系统接线图、实物图和演示视频
